# Webhook.site test provider configuration
# This file is automatically scanned by CaptainHook on "Scan for Providers"

name: webhook_site
display_name: Webhook.site
description: Test webhook provider for development
adapter_file: webhook_site.rb
active: true

# Security settings
signing_secret: ENV[WEBHOOK_SITE_SECRET]

# Rate limiting (optional)
rate_limit_requests: 1000
rate_limit_period: 60
