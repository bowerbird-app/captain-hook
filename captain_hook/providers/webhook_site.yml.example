# Webhook.site testing provider configuration
# This file is automatically scanned by CaptainHook on "Scan for Providers"
# Place this file in: captain_hook/providers/webhook_site/webhook_site.yml
# Note: webhook_site.rb adapter is built into CaptainHook - no need to create it!

name: webhook_site
display_name: Webhook.site
description: Testing provider for webhook.site (no signature verification)
adapter_file: webhook_site.rb
active: true

# No signing secret needed for webhook.site
signing_secret: skip

# Rate limiting (optional)
rate_limit_requests: 1000
rate_limit_period: 60

# Payload size limit (optional, in bytes)
max_payload_size_bytes: 1048576
