# Square webhook provider configuration
# This file is automatically scanned by CaptainHook on "Scan for Providers"
# Place this file in: captain_hook/providers/square/square.yml
#
# CaptainHook includes a built-in Square adapter, so you don't need to create square.rb
# Just set adapter_class to use the built-in adapter

name: square
display_name: Square
description: Square payment and order webhooks
adapter_class: CaptainHook::Adapters::Square
active: true

# Security settings
signing_secret: ENV[SQUARE_WEBHOOK_SECRET]
timestamp_tolerance_seconds: 300

# Rate limiting (optional)
rate_limit_requests: 100
rate_limit_period: 60

# Payload size limit (optional, in bytes)
max_payload_size_bytes: 1048576
