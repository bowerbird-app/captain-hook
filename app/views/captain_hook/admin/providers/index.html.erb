<div class="container py-4">
  <div class="d-flex justify-content-between align-items-start mb-4">
    <h1>Webhook Providers</h1>
    <div class="d-flex gap-2">
      <span data-bs-toggle="tooltip" data-bs-placement="bottom" data-bs-title="Add new providers/actions only, don't update existing ones">
        <%= button_to "Discover New", discover_new_admin_providers_path, method: :post, class: "btn btn-outline-primary", data: { confirm: "This will scan for NEW providers/actions only. Existing providers will not be updated. Continue?" } %>
      </span>
      <span data-bs-toggle="tooltip" data-bs-placement="bottom" data-bs-title="Update all providers/actions from YAML files">
        <%= button_to "Full Sync", sync_all_admin_providers_path, method: :post, class: "btn btn-primary", data: { confirm: "This will update ALL providers/actions from YAML files. Any manual changes will be overwritten. Continue?" } %>
      </span>
    </div>
  </div>

  <div class="card">
    <div class="card-body">
      <% if @providers.any? %>
        <div class="table-responsive">
          <table class="table table-hover">
            <thead>
              <tr>
                <th>Name</th>
                <th>Display Name</th>
                <th>Status</th>
                <th>Security</th>
                <th>Events</th>
                <th>Created</th>
                <th>Actions</th>
              </tr>
            </thead>
            <tbody>
              <% @providers.each do |provider| %>
                <tr>
                  <td>
                    <code><%= provider.name %></code>
                  </td>
                  <td><%= provider.display_name || provider.name.titleize %></td>
                  <td>
                    <% if provider.active? %>
                      <span class="badge bg-success">Active</span>
                    <% else %>
                      <span class="badge bg-secondary">Inactive</span>
                    <% end %>
                  </td>
                  <td>
                    <% if provider.signing_secret.present? %>
                      <span class="badge bg-success">Configured</span>
                    <% else %>
                      <span class="badge bg-secondary">No Secret</span>
                    <% end %>
                  </td>
                  <td><%= provider.incoming_events.count %></td>
                  <td><%= time_ago_in_words(provider.created_at) %> ago</td>
                  <td>
                    <%= link_to "View", [:admin, provider], class: "btn btn-sm btn-outline-primary" %>
                    <%= link_to "Edit", edit_admin_provider_path(provider), class: "btn btn-sm btn-outline-secondary" %>
                    <%= link_to "Actions", admin_provider_actions_path(provider), class: "btn btn-sm btn-outline-info" %>
                  </td>
                </tr>
              <% end %>
            </tbody>
          </table>
        </div>

        <!-- Pagination -->
        <div class="mt-3">
          <%= paginate @providers %>
        </div>
      <% else %>
        <div class="alert alert-info">
          <p class="mb-2"><strong>No providers configured yet.</strong></p>
          <p class="mb-0">Providers are discovered from YAML files in the <code>captain_hook/providers/</code> directory. <%= button_to "Discover providers", discover_new_admin_providers_path, method: :post, class: "btn btn-sm btn-primary d-inline-block", data: { confirm: "This will scan for NEW provider YAML files. Continue?" } %> to discover and create them.</p>
        </div>
      <% end %>
    </div>
  </div>

  <!-- Quick Info -->
  <div class="card mt-4">
    <div class="card-body">
      <h5 class="card-title">About Providers</h5>
      <p class="card-text">
        Providers represent external services that send webhooks to your application.
        Each provider has a unique webhook URL that you can share with the service.
      </p>
      <p class="card-text">
        <strong>Provider Discovery:</strong> CaptainHook automatically discovers providers from YAML files.
        Create provider configuration files in <code>captain_hook/providers/</code> directory in your Rails app
        or in any gem. Click "Scan for Providers" to discover and create them.
      </p>
      <ul>
        <li><strong>Name:</strong> Unique identifier for the provider (lowercase, underscores only)</li>
        <li><strong>Verifier:</strong> Handles signature verification for this provider</li>
        <li><strong>Token:</strong> Automatically generated secure token for the webhook URL</li>
        <li><strong>Actions:</strong> Ruby classes that process events from this provider</li>
        <li><strong>Configuration:</strong> Define providers in YAML with signing secrets from ENV variables</li>
      </ul>
    </div>
  </div>
</div>
