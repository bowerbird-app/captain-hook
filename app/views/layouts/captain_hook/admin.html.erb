<!DOCTYPE html>
<html>
  <head>
    <title>CaptainHook Admin</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <!-- Tailwind CSS with FlatPack styles -->
    <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "captain_hook/variables", "data-turbo-track": "reload" %>
    
    <!-- JavaScript -->
    <%= javascript_importmap_tags %>
    
    <%= yield :head %>
  </head>

  <body class="bg-gray-50">
    <!-- Navigation -->
    <nav class="bg-gray-900 text-white">
      <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="flex items-center justify-between h-16">
          <div class="flex items-center space-x-8">
            <a href="<%= captain_hook.admin_root_path %>" class="text-xl font-semibold">
              ⚓ CaptainHook
            </a>
            <div class="flex space-x-4">
              <%= link_to "Providers", captain_hook.admin_providers_path, 
                  class: "px-3 py-2 rounded-md text-sm font-medium #{controller_name == 'providers' ? 'bg-gray-700 text-white' : 'text-gray-300 hover:bg-gray-700 hover:text-white'}" %>
              <%= link_to "Incoming Events", captain_hook.admin_incoming_events_path, 
                  class: "px-3 py-2 rounded-md text-sm font-medium #{controller_name == 'incoming_events' ? 'bg-gray-700 text-white' : 'text-gray-300 hover:bg-gray-700 hover:text-white'}" %>
              <%= link_to "Sandbox", captain_hook.admin_sandbox_path, 
                  class: "px-3 py-2 rounded-md text-sm font-medium #{controller_name == 'sandbox' ? 'bg-gray-700 text-white' : 'text-gray-300 hover:bg-gray-700 hover:text-white'}" %>
            </div>
          </div>
        </div>
      </div>
    </nav>

    <!-- Flash Messages -->
    <% if flash.any? %>
      <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 mt-4">
        <% flash.each do |type, message| %>
          <% 
            alert_classes = case type.to_s
            when 'notice' then 'bg-blue-50 border-blue-500 text-blue-900'
            when 'alert' then 'bg-red-50 border-red-500 text-red-900'
            when 'warning' then 'bg-yellow-50 border-yellow-500 text-yellow-900'
            when 'success' then 'bg-green-50 border-green-500 text-green-900'
            else 'bg-blue-50 border-blue-500 text-blue-900'
            end
          %>
          <div class="border-l-4 p-4 mb-4 <%= alert_classes %> rounded" role="alert">
            <%= simple_format(message, {}, wrapper_tag: "div", sanitize: false) %>
          </div>
        <% end %>
      </div>
    <% end %>

    <!-- Main Content -->
    <%= yield %>
  </body>
</html>
